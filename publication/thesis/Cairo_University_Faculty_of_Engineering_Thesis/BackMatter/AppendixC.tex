\setstretch{1.5}

This appendix reproduces the key sections of the runtime configuration file
\texttt{configs/config.yaml} used for all experiments reported in this thesis.
The full file is available in the project repository.

%────────────────────────────────────────────────────────────────────────────
\section*{C.1 \quad Model and Evaluation Settings}
%────────────────────────────────────────────────────────────────────────────

\begin{lstlisting}
model:
  name: "Qwen/Qwen3-4B-Instruct-2507"
  backend: "transformers"
  temperature: 0.1
  max_tokens: 1024
  device: "auto"
  quantize_4bit: false

evaluation:
  strip_diacritics: true
  report_both: true
\end{lstlisting}

%────────────────────────────────────────────────────────────────────────────
\section*{C.2 \quad Dataset Configuration (18 datasets)}
%────────────────────────────────────────────────────────────────────────────

\begin{lstlisting}
datasets:
  - name: "PATS-A01-Akhbar-train"
    font: "Akhbar"
    type: "PATS-A01"
    pats_split: "train"
  - name: "PATS-A01-Akhbar-val"
    font: "Akhbar"
    type: "PATS-A01"
    pats_split: "validation"
  # ... (repeated for Andalus, Arial, Naskh, Simplified,
  #      Tahoma, Thuluth, Traditional -- 8 fonts x 2 splits)
  - name: "KHATT-train"
    split: "train"
    type: "KHATT"
  - name: "KHATT-validation"
    split: "validation"
    type: "KHATT"
\end{lstlisting}

%────────────────────────────────────────────────────────────────────────────
\section*{C.3 \quad Phase-Specific Settings}
%────────────────────────────────────────────────────────────────────────────

\begin{lstlisting}
phase3:
  top_n: 10
  format_style: "flat_arabic"
  min_substitutions: 200

phase4:
  rules:
    format_style: "compact_arabic"
  few_shot:
    num_examples: 5
    selection: "diverse"
    max_length: 300
    min_length: 10
    max_words_changed: 15
  camel_validation:
    strategy: "revert"

phase4d:
  source_phase: "phase2"
  insights:
    max_fix_rate_weakness: 0.4
    min_intro_rate: 0.05
    min_sample_size: 10
    top_n_weaknesses: 3
    top_n_overcorrections: 2

phase5:
  corpus:
    max_sentences: 200000
    min_char_len: 30
    max_char_len: 300
    status_filter: "pri"
  retrieval:
    top_k: 3
    format_style: "numbered_arabic"

rag:
  embedding_model: >
    sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
  top_k: 3

phase6:
  stats:
    alpha: 0.05
    n_bootstrap: 1000

camel:
  enabled: true
  morphology:
    db: "calima-msa-r13"
    cache_size: 10000
\end{lstlisting}
